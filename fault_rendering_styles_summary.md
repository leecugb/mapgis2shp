# 断裂构造渲染样式总表

> 状态：**临时方案**。类型代码地质含义待图例最终确认；确认后仅需修改 
`fault_rendering_styles.json` 中的 `type_map`。

## 1. 全局参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `short_segment_threshold_m` | 5000.0 | |
| `named_fault_merge` | True | |
| `dip_angle_min` | 0.1 | |
| `dip_angle_max_display` | 90.0 | |
| `abnormal_dip_highlight` | 90.0 | |

## 2. 数据源与基础线型

| 数据源 | 文件 | 类型字段 | 倾角字段 | 线色 | 线宽 | 线型 | 透明度 |
|--------|------|----------|----------|------|------|------|--------|
| `LDZOFBA003` | `LDZOFBA003.WL` | GZEEB | GZECE | #e41a1c | 0.7 | - | 1.0 |
| `LYGREBA001` | `LYGREBA001.WL` | GZEEBM, GZEGD | GZEEI | #e41a1c | 0.6 | -. | 0.9 |
| `LZLPGDJ002` | `LZLPGDJ002.WL` | - | GZEEL | #e41a1c | 1.5 | -- | 1.0 |

## 3. 实测断层 `LDZOFBA003.WL` 类型代码映射

| 代码 | 符号类型 | 默认倾向侧 | 标注倾角 | 线型 | 说明 |
|------|----------|------------|----------|------|------|
| `01` | `solid_unknown` | 无 | 否 | - | 实测性质不明断层（DZ/T 0179 表22） |
| `05` | `normal` | right | 是 | - | 实测正断层（暂定）；GZELD=101 与之完全绑定 |
| `31` | `reverse` | right | 是 | - | 实测逆断层/冲断层/推覆断层（暂定）；三角齿 |
| `04` | `concealed` | 无 | 否 | -- | 推测性质不明断层 / 隐伏断层（DZ/T 0179 表22） |
| `41` | `shear_zone_double_line` | 无 | 否 | - | 韧性剪切带/断层破碎带（红色双线，DZ/T 0179 表22） |
| `28` | `shear_zone_double_line` | 无 | 否 | - | 断层破碎带/脆韧性剪切带 |
| `07` | `solid_unknown` | 无 | 否 | - | 实测性质不明断层（DZ/T 0179 表22） |
| `18` | `active` | right | 否 | - | 实测活动断层（暂定）；双箭头 |
| `16` | `active` | right | 否 | - | 实测活动断层（暂定）；双箭头 |

## 4. 解译断层 `LYGREBA001.WL` 类型代码映射

| GZEEBM/GZEGD | 符号类型 | 默认倾向侧 | 标注倾角 | 线型 | 说明 |
|--------------|----------|------------|----------|------|------|
| `01,04` | `reverse` | right | 是 | -. | 航卫片解译断层 / 逆冲（DZ/T 0179 表22 点划线） |
| `02,03` | `normal` | right | 是 | -. | 航卫片解译断层 / 正断层（暂定） |
| `05,05` | `concealed` | 无 | 否 | -- | 隐伏或物探推测断层（DZ/T 0179 表22 长虚线+点） |

## 5. 深部大断裂 `LZLPGDJ002.WL`

| 处理方式 | 线色 | 线宽 | 线型 | 符号 | 注记 |
|----------|------|------|------|------|------|
| 全部记录 | #e41a1c | 1.5 | -- | 无 | 沿线标注名称 |

## 6. 命名断层特殊处理

| 数据源 | 名称 | 合并线段 | 标注位置 | 备注 |
|--------|------|----------|----------|------|
| `LDZOFBA003` | F48 | 是 | middle | 含 05/28 两种代码，按段分别渲染符号 |
| `LDZOFBA003` | F50 | 是 | middle | 含 04/41 两种代码，04 用虚线、41 用破碎带双线 |
| `LDZOFBA003` | F9 | 是 | middle | 全部为 41，用破碎带双线 |
| `LYGREBA001` | 乌赤别里山口断裂 | 是 | inflection_outer | 西段齿指向北，东段齿指向南；南侧局部倒转 |
| `LYGREBA001` | 布伦口断裂 | 是 | middle | 齿指向北东，倾角 60–70° |

## 7. 符号几何定义

| 符号类型 | 几何描述 | 填充 |
|----------|----------|------|
| `solid_unknown` | 实测性质不明断层：仅实线，不画侧向符号 | 否 |
| `normal` | 正断层：下降盘短齿 | 否 |
| `reverse` | 逆断层/冲断层/推覆断层：上盘三角齿 | 是 |
| `thrust` | 推覆断层：上盘三角齿（同 reverse） | 是 |
| `strike_slip` | 平推断层：半箭头 | 否 |
| `active` | 活动断层：双箭头 | 否 |
| `concealed` | 隐伏/推测断层：长虚线+点 | 否 |
| `shear_zone_double_line` | 韧性/脆韧性剪切带：红色平行双线 | 否 |
| `interpreted` | 航卫片解译断层：点划线 | 否 |
| `neutral_tick` | 短垂线，不指定盘侧 | 否 |
| `arc_unknown` | 弧形/性质不明断层：虚线+中点短垂线 | 否 |
| `none` | 不绘制符号 | 否 |

## 8. 使用方式

1. 运行 `python3 build_fault_rendering_styles.py` 重新生成本表与 JSON 配置；

2. 在 `render_strict_standard_map.py` 中加载 `fault_rendering_styles.json`；

3. 按数据源遍历断层线，依据 `type_map` 选择符号类型与线型；

4. 图例确认后，修改 `type_map` 中的 `symbol_type` / `default_side` / `note` 并重新渲染。

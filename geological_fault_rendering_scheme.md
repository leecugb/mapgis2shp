# 断裂产状渲染方案

> **状态**：类型代码地质含义尚未由图例最终确认，本方案采用“**临时符号映射 + 明确待确认**”策略，先保证图面不误导，待图例确定后仅替换映射表即可。

## 1. 数据来源（已修正）

图幅内与断裂产状相关的数据分布在三个线文件中：

| 文件 | 类型 | 记录数 | 数据性质 | 关键字段 |
|------|------|--------|----------|----------|
| `LDZOFBA003.WL` | 线 | 310 | 实测/填图断层迹线 | `GZEAB` 断层编号、`GZEEB` 类型代码、`GZECE` 断面倾角、`GZECD` 断面倾向、`GZEHG` 描述 |
| `LYGREBA001.WL` | 线 | 71 | 遥感/地质解译断层 | `GZEAB` 断层名、`GZEEBM`/`GZEGD` 类型代码、`GZEEI` 倾角、`GZEAD` 描述 |
| `LZLPGDJ002.WL` | 线 | 3 | 深部/物探推断大断裂 | `GZEAB` 断层名、`GZECC` 走向文字、`GZEEL` 倾角 |

完整属性梳理见 [`fault_type_geometry_summary.md`](fault_type_geometry_summary.md)。

## 2. 渲染总体原则

1. **分层渲染**：实测、解译、深部三类断层必须区分线型与透明度。
2. **类型优先**：`GZEEB`/`GZEEBM`/`GZEGD` 决定符号样式；倾角只作为数值注记，不决定符号类型。
3. **倾向侧不可默认右侧**：无倾向信息时，不画指向性符号（齿、箭头），仅绘制断层迹线或中性标记。
4. **零倾角不标**：`GZECE=0`、`GZEEL=0` 或缺失时，不标注倾角。
5. **命名断层先合并**：`F48`、`F50`、`F9` 等被拆分为多条线段，渲染前按 `GZEAB` 合并或统一采样，避免符号断裂/重复。

## 3. 临时类型代码映射

> 下列映射为基于描述与数量的**暂定推断**，必须在图例确认后替换为正式映射。建议将映射表提取为独立字典/JSON，方便后续一键替换。

### 3.1 实测断层 `LDZOFBA003.WL`（`GZEEB`）

| 代码 | 数量 | 典型描述 | 暂定类型 | 符号 | 倾向侧策略 |
|------|------|----------|----------|------|------------|
| `01` | 155 | `ChSt.`、`D1s,D2t` | 一般/性质不明断层 | 短垂线（中性） | 无信息时不画侧向符号；有文字描述时按描述侧绘制 |
| `05` | 49 | `D2t,D1s`，`GZELD=101` | 正断层（暂定） | 下降盘短齿 | 无 `GZECD` 时默认右侧，并在图例注明“倾向侧待确认” |
| `31` | 26 | `ChA.,K1kz` | 逆断层/推覆断层（暂定） | 上盘三角齿 | 默认右侧，图例注明暂定 |
| `04` | 25 | `Qsn` | 第四系断层/隐伏断层 | 虚线 + 小点 | 不画倾向符号 |
| `41` | 21 | 切割多套地层，碎裂岩/糜棱岩 | 韧性剪切带/断层破碎带 | 双线或粗线 + 齿 | 不强调倾向侧 |
| `28` | 20 | 碎裂岩、构造角砾岩、糜棱岩 | 断层破碎带 | 双线或粗线 | 不强调倾向侧 |
| `07` | 10 | `ChSt.,D2kz` | 性质不明断层 | 短垂线（中性） | 不画侧向符号 |
| `18` | 3 | `EK,N1k,N1a` | 新生代断层 | 短虚线 + 短齿 | 默认右侧 |
| `16` | 1 | `N1k,N1a` | 新生代断层 | 短虚线 + 短齿 | 默认右侧 |

### 3.2 解译断层 `LYGREBA001.WL`（`GZEEBM`/`GZEGD`）

| `GZEEBM` | `GZEGD` | 数量 | 典型遥感特征 | 暂定类型 | 符号 | 倾向侧策略 |
|----------|---------|------|--------------|----------|------|------------|
| `01` | `04` | 36 | 波状纹理清晰 | 逆断层/走滑逆冲（暂定） | 上盘三角齿 | 命名断层按描述；其余默认右侧 |
| `02` | `03` | 30 | 线状纹理明显 | 正断层（暂定） | 下降盘短齿 | 命名断层按描述；其余默认右侧 |
| `05` | `05` | 5 | 弧形纹理明显 | 性质不明/弧形断层 | 虚线 + 中点短垂线 | 不画指向性符号 |

> 命名解译断层的描述提供倾向信息：
> - **乌赤别里山口断裂**：西段向北倾，东段向南倾；南侧局部倒转，石炭系逆掩于北侧白垩系、第三系之上。
> - **布伦口断裂**：断面倾向北东，倾角 60–70°。

### 3.3 深部大断裂 `LZLPGDJ002.WL`

- 线型：粗红色虚线（`linewidth=1.5`, `linestyle='--'`）。
- 符号：不绘制倾向齿/箭头，`GZEEL` 全为 0。
- 注记：沿线标注断层名称（乌恰县大断裂、阿克奇－奎巴格镇大断裂、昆仑山深大断裂）。

## 4. 几何符号绘制规则

### 4.1 采样间距

- **实测断层**：1:250000 图幅，建议每 **5–7 km** 采样一个符号；短于 5 km 的线段只在中点绘制一次。
- **解译断层**：迹线较长且稀疏，建议每 **12–15 km** 采样一次；短于 10 km 只在中点绘制。
- **命名断层**：先按 `GZEAB` 合并所有线段，再按上述间距统一采样。

### 4.2 倾向侧判定（优先级）

1. **显式倾向字段**：`GZECD`（实测）或 `GZEAD`/`GZEHG` 中解析出的倾向方位（北/南/东/西/北东等）。
2. **类型代码映射**：若图例确认某代码对应正/逆断层，按标准符号侧绘制；无确认时跳过此步。
3. **默认规则**：对允许使用默认侧的暂定类型（如 `05`、`31`、`18`、`16`），统一绘制在断层线前进方向的**右侧**，并在图例显著位置声明“倾向侧为暂定”。
4. **不绘制侧向符号**：对“性质不明”类型（`01`、`07`、解译 `05/05`）或无任何倾向信息时，仅绘制断层迹线，或在中性点绘制一个小圆点标记。

### 4.3 符号尺寸与颜色

| 数据源 | 线色 | 线宽 | 线型 | 符号颜色 | 符号大小 |
|--------|------|------|------|----------|----------|
| 实测断层 | `#1a1a1a`（近黑） | 0.7 | 实线 | `#000000` | 齿长 250–400 m（随倾角略增） |
| 解译断层 | `#7a3e9a`（紫） | 0.6 | 点划线 `-.` | `#000000` | 齿长 200–350 m |
| 深部断裂 | `#c41e3a`（红） | 1.5 | 虚线 `--` | — | — |

符号长度可随倾角微调：

```python
tick_len_m = 250.0 + 150.0 * min(dip, 90.0) / 90.0
```

### 4.4 倾角注记

- 仅当倾角字段 > 0 时标注：
  - `LDZOFBA003.WL`：`GZECE > 0` 的 59 条；
  - `LYGREBA001.WL`：`GZEEI > 0` 的 71 条；
  - `LZLPGDJ002.WL`：不标注。
- 注记位置：符号末端沿断层线再偏移 150–250 m。
- 字体：黑色，字号 3.0–3.5 pt，水平居中，`ha='center'`, `va='center'`。
- 对异常倾角（如 `GZEEI > 90°`），保留原值但可标注为 `>80` 或在图例中说明“含陡倾/倒转”。

## 5. 命名断层特殊处理

| 名称 | 数据源 | 处理建议 |
|------|--------|----------|
| `F48` | `LDZOFBA003.WL` | 合并 15 段后绘制；含 `05`/`28` 两种代码，按段分别渲染符号；在总长度中点附近标注名称 |
| `F50` | `LDZOFBA003.WL` | 合并 22 段后绘制；含 `04`/`41`，`04` 用虚线，`41` 用破碎带双线；标注名称 |
| `F9` | `LDZOFBA003.WL` | 合并 5 段后绘制；全部为 `41`，用破碎带双线；标注名称 |
| 乌赤别里山口断裂 | `LYGREBA001.WL` | 合并 2 段；西段齿指向北，东段齿指向南；名称注记在拐点外侧 |
| 布伦口断裂 | `LYGREBA001.WL` | 合并 2 段；齿指向北东；标注名称与倾角 60–70° |
| 乌恰县/阿克奇/昆仑山深大断裂 | `LZLPGDJ002.WL` | 粗虚线 + 名称沿线标注 |

## 6. Python 实现要点

建议将 `render_strict_standard_map.py` 中的 `draw_fault_attitudes()` 拆分为以下步骤：

1. **合并命名断层**：按 `GZEAB` 将 `MultiLineString` 或分散 `LineString` 合并为统一的 `GeometryCollection`/`MultiLineString`，便于统一采样。
2. **类型映射字典**：
   ```python
   FAULT_SYMBOL_MAP = {
       # source: {code: (symbol_type, default_side, has_dip_tick)}
       "LDZOFBA003": {
           "01": ("neutral_tick", None, False),
           "05": ("normal", "right", True),
           "31": ("reverse", "right", True),
           "04": ("dashed", None, False),
           "41": ("shear_zone", None, False),
           "28": ("shear_zone", None, False),
           "07": ("neutral_tick", None, False),
           "18": ("normal", "right", True),
           "16": ("normal", "right", True),
       },
       "LYGREBA001": {
           ("01", "04"): ("reverse", "right", True),
           ("02", "03"): ("normal", "right", True),
           ("05", "05"): ("arc_unknown", None, False),
       },
   }
   ```
3. **倾向侧计算**：
   - 对默认 `right` 侧，取切线方向 `(ux, uy)`，右侧法向量为 `(uy, -ux)`。
   - 对文字描述（如“倾向北东”），将方位词转换为方位角（北=0°，北东=45°，东=90°…），选择法向量中与该方位夹角更小的一侧。
4. **符号绘制函数**：
   - `draw_normal_tick(ax, x0, y0, nx, ny, tick_len)`
   - `draw_reverse_tooth(ax, x0, y0, ux, uy, nx, ny, tooth_len)`（绘制小三角齿）
   - `draw_neutral_tick(ax, ...)`（短垂线，不指定盘侧）
   - `draw_dip_label(ax, x, y, dip, ux, uy, offset)`
5. **异常值处理**：`dip > 90` 时仍注记原值，但颜色可改为深红以提示需复核。

## 7. 输出核验

渲染后应运行两级核验：

1. **图面抽样检查**：在主要断层密集区目视检查符号是否沿断层线均匀、是否与相邻地层关系协调。
2. **属性核验**：
   - 统计每种 `GZEEB`/`GZEEBM` 代码实际绘制的符号数量，与记录数比对；
   - 检查 `GZECE=0` 的线段是否被错误标注；
   - 检查 `LYGREBA001.WL` 中 71 条记录的倾角是否全部注记。

建议新增 `verify_rendered_faults.py`，读取渲染日志或重新采样输出 PNG，生成核验报告。

## 8. 待图例确认后的替换方式

一旦图例/数据字典确认类型代码含义，只需修改 `FAULT_SYMBOL_MAP`：

- 将暂定类型替换为正断层、逆断层、走滑断层、平移断层、推覆断层等标准符号；
- 更新 `default_side` 为地质正确的盘侧；
- 删除图例中的“倾向侧暂定”声明；
- 重新运行 `render_strict_standard_map.py` 与 `verify_rendered_faults.py`。

## 9. 输出产物

- 本方案：`geological_fault_rendering_scheme.md`
- 属性梳理：`fault_type_geometry_summary.md`
- 实施代码：待图例确认后在 `render_strict_standard_map.py` 中更新 `draw_fault_attitudes()`
